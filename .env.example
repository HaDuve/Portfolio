# Copy to .env on VM1 only — do not commit .env
# Created automatically by scripts/deploy-remote.sh on first deploy.
ANALYTICS_INGEST_SECRET=generate-a-long-random-string
ANALYTICS_IP_HASH_SALT=generate-another-long-random-string
# Optional (comma-separated)
# ANALYTICS_ALLOWED_ORIGINS=https://hannesduve.com
#
# Click retention: rows older than 12 months are removed by prune-clicks (see README).
#   docker compose run --rm analytics node dist/prune-clicks.js
